<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="prosayj.mybatis.plugin.mapper.UserMapper">
    <select id="selectUsers" resultType="prosayj.mybatis.plugin.pojo.User">
        SELECT id, username
        FROM user
    </select>

</mapper>